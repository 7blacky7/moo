# Modul mit Variablen die von Funktionen gebraucht werden
setze PI auf 3.14159
konstante FAKTOR auf 2.5

funktion kreis_flaeche(r):
    gib_zurück PI * r * r

funktion kreis_umfang(r):
    gib_zurück 2 * PI * r

funktion skaliere(x):
    gib_zurück x * FAKTOR
