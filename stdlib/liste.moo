# moo Listen-Bibliothek

funktion summe(l):
    setze s auf 0
    für x in l:
        setze s auf s + x
    gib_zurück s

funktion produkt(l):
    setze p auf 1
    für x in l:
        setze p auf p * x
    gib_zurück p
