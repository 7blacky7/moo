name: UI Module Cross-Platform CI

# ============================================================================
# ui-ci.yml — Cross-Platform-Build fuer das moo UI-Modul.
#
# Strategie (Plan plan-002-moo-ui-cross-platform, Phase 3b):
#   - Linux   (ubuntu-latest)  → GTK3 + libappindicator3, voller Smoke-Test
#                                mit `timeout` gegen moo_ui_gtk.c
#   - Windows (windows-latest) → Win32-SDK bereits im Image, testet
#                                moo_ui_win32.c + den P013-Kern-Port
#                                (Winsock2, CreateProcess-Pfade, win_regex)
#   - macOS   (macos-latest)   → Cocoa-Frameworks im Image, testet
#                                moo_ui_cocoa.m   (Phase 4, blind geschrieben)
#
# Wir bauen mit `--no-default-features --features moo_ui`, d.h. OHNE gl33/glfw,
# um die UI-Fehler zu isolieren. Das 3D-Backend hat seinen eigenen
# Workflow (3d-backends.yml).
#
# P013-Stand (2026-06-12): Die komplette Kern-Runtime kompiliert fuer
# Windows — alle POSIX-Pfade (moo_net/web/eval/video/regex) sind portiert
# und MinGW-verifiziert (x86_64-w64-mingw32-gcc, 0 Fehler; Details:
# Synapse-Plan P013 + runtime/win_regex/README.md). DIESER Job baut mit
# der MSVC-Toolchain (windows-msvc + vcpkg) und bleibt experimental, bis
# der erste Lauf gruen ist — moegliche Reste sind MSVC-spezifische
# Diagnosen im vendorten musl/TRE-Regex. macOS ist weiterhin blind
# geschrieben; ui-cocoa iteriert auf Basis der GitHub-Actions-Logs.
# ============================================================================

on:
  push:
    branches: [master, main]
    paths:
      - "compiler/src/**"
      # Der Job baut+linkt die KOMPLETTE Kern-Runtime (libmoo_runtime.a) —
      # jede runtime-Datei kann den Link brechen (Lehre: cf12afc/moo_stdlib.c
      # triggerte keinen Run, CLOCK_MONOTONIC-Fix blieb unbewiesen).
      - "compiler/runtime/**"
      - "compiler/Cargo.toml"
      - "compiler/build.rs"
      - "beispiele/ui_login_demo.moos"
      - "beispiele/tests/**"
      - "scripts/ui-test-runner.sh"
      - "stdlib/ui*.moos"
      - ".github/workflows/ui-ci.yml"
  pull_request:
    paths:
      - "compiler/src/**"
      - "compiler/runtime/**"
      - "compiler/Cargo.toml"
      - "compiler/build.rs"
      - "beispiele/ui_login_demo.moos"
      - "beispiele/tests/**"
      - "scripts/ui-test-runner.sh"
      - "stdlib/ui*.moos"
      - ".github/workflows/ui-ci.yml"
  workflow_dispatch:

jobs:
  ui-build:
    name: ${{ matrix.os }} / ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    # Windows/macOS duerfen fehlschlagen ohne den ganzen Workflow zu killen:
    continue-on-error: ${{ matrix.experimental }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            exe_suffix: ""
            experimental: false
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            exe_suffix: ".exe"
            experimental: true
          - os: macos-latest
            # macos-latest laeuft auf Apple Silicon — der Job baut nativ arm64.
            # Vorher stand hier faelschlich x86_64-apple-darwin (Label-Luege).
            target: aarch64-apple-darwin
            exe_suffix: ""
            experimental: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ---------------------------------------------------------------------
      # Plattform-spezifische System-Dependencies
      # ---------------------------------------------------------------------
      - name: Install Linux deps (GTK3, LLVM 18, SDL2, curl, sqlite)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          # WICHTIG: LLVM-Version muss zu `features = ["llvm18-1"]` in
          # compiler/Cargo.toml passen (llvm-sys 181). Die Default-LLVM auf
          # ubuntu-latest ist v14 — wir brauchen explizit v18.
          sudo apt-get install -y \
            llvm-18-dev libpolly-18-dev libclang-18-dev \
            libgtk-3-dev libappindicator3-dev \
            libsdl2-dev libsdl2-image-dev \
            libcurl4-openssl-dev libsqlite3-dev \
            pkg-config
          echo "LLVM_SYS_181_PREFIX=/usr/lib/llvm-18" >> $GITHUB_ENV
          echo "LIBCLANG_PATH=/usr/lib/llvm-18/lib" >> $GITHUB_ENV

      # Windows: Der offizielle LLVM-Windows-Installer enthaelt kein
      # llvm-config.exe, und vcpkg baut LLVM aus Source (zu langsam/instabil).
      # Deshalb nutzen wir conda-forge fuer eine vorgebaute LLVM-Dev-Distribution
      # mit echtem llvm-config.exe. vcpkg bleibt nur fuer SDL/curl/sqlite.
      - name: Install Windows LLVM (conda-forge, real llvm-config)
        if: matrix.os == 'windows-latest'
        uses: mamba-org/setup-micromamba@v2
        with:
          environment-name: moo-llvm
          create-args: >-
            -c conda-forge
            llvmdev=18.1.*
            clangdev=18.1.*
            lld=18.1.*
            zstd
            zlib
            libxml2
            libxml2-devel
          cache-environment: true

      # Stellt VC- und Windows-SDK-Pfade (u.a. ws2_32.lib) auch fuer den
      # spaeter vom Moo-Compiler gestarteten clang/link.exe-Aufruf bereit.
      - name: Configure MSVC developer environment
        if: matrix.os == 'windows-latest'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install Windows deps (SDL2, sqlite, curl via vcpkg + LLVM env)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $condaPrefix = $env:CONDA_PREFIX
          if (!$condaPrefix) {
            $condaPrefix = Join-Path $env:MAMBA_ROOT_PREFIX "envs\moo-llvm"
          }
          if (!(Test-Path $condaPrefix)) {
            Write-Host "CONDA_PREFIX=$env:CONDA_PREFIX"
            Write-Host "MAMBA_ROOT_PREFIX=$env:MAMBA_ROOT_PREFIX"
            throw "Could not locate moo-llvm conda environment"
          }
          $llvmConfig = Join-Path $condaPrefix "Library\bin\llvm-config.exe"
          if (!(Test-Path $llvmConfig)) {
            $llvmConfig = Join-Path $condaPrefix "bin\llvm-config.exe"
          }
          if (!(Test-Path $llvmConfig)) {
            Write-Host "=== conda prefix layout ==="
            Get-ChildItem -Path $condaPrefix | Format-Table
            if (Test-Path (Join-Path $condaPrefix "Library\bin")) {
              Write-Host "=== Library/bin ==="
              Get-ChildItem -Path (Join-Path $condaPrefix "Library\bin") | Select-Object -First 80 | Format-Table
            }
            throw "conda-forge LLVM did not provide llvm-config.exe"
          }

          Write-Host "=== conda LLVM prefix (${condaPrefix}) ==="
          & $llvmConfig --version
          Write-Host "prefix: $(& $llvmConfig --prefix)"
          Write-Host "libdir: $(& $llvmConfig --libdir)"
          Write-Host "includedir: $(& $llvmConfig --includedir)"

          # SDL2 + SDL2_image + curl + sqlite via vcpkg.
          vcpkg install --triplet x64-windows `
            sdl2 sdl2-image curl sqlite3

          $vcpkgRoot = "$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows"

          # P013: llvm-config --system-libs des conda-forge-LLVM referenziert
          # zstd.dll.lib (LNK1181). Sicherstellen, dass die Import-Libs unter
          # genau diesen Namen im LIB-Pfad liegen — sonst Alias-Kopie.
          $condaLib = Join-Path $condaPrefix "Library\lib"
          foreach ($paar in @(@("zstd.lib","zstd.dll.lib"), @("zlib.lib","z.dll.lib"))) {
            $quelle = Join-Path $condaLib $paar[0]
            $ziel   = Join-Path $condaLib $paar[1]
            if ((Test-Path $quelle) -and !(Test-Path $ziel)) {
              Copy-Item $quelle $ziel
              Write-Host "Alias angelegt: $($paar[1]) <- $($paar[0])"
            }
          }
          Write-Host "=== zstd/zlib-Libs im conda-Env ==="
          Get-ChildItem $condaLib -Filter "*zstd*" -ErrorAction SilentlyContinue | Format-Table Name
          Get-ChildItem $condaLib -Filter "*zlib*" -ErrorAction SilentlyContinue | Format-Table Name
          Get-ChildItem $condaLib -Filter "z*.lib" -ErrorAction SilentlyContinue | Format-Table Name

          # P013 generisch: jede von llvm-config --system-libs referenzierte
          # Lib muss unter genau diesem Namen im LIB-Pfad liegen. conda-forge
          # benennt Import-Libs teils anders (libxml2.lib vs. xml2.lib) --
          # fehlende Namen als Alias-Kopie der ersten passenden Variante.
          $sysLibs = ((& $llvmConfig --system-libs) -split '\s+') | Where-Object { $_ }
          Write-Host "llvm-config --system-libs: $($sysLibs -join ' ')"
          foreach ($lib in $sysLibs) {
            $name = [IO.Path]::GetFileName($lib)
            if ($name -notlike '*.lib') { continue }
            $ziel = Join-Path $condaLib $name
            if (Test-Path $ziel) { continue }
            $basis = $name -replace '\.lib$','' -replace '\.dll$',''
            foreach ($k in @("lib$basis.lib", "$basis.lib", "$basis.dll.lib", "lib$basis.dll.lib")) {
              $quelle = Join-Path $condaLib $k
              if (Test-Path $quelle) { Copy-Item $quelle $ziel; Write-Host "Alias: $name <- $k"; break }
            }
            if (!(Test-Path $ziel)) {
              $treffer = Get-ChildItem $condaLib -Filter "*$basis*.lib" -ErrorAction SilentlyContinue | Select-Object -First 1
              if ($treffer) { Copy-Item $treffer.FullName $ziel; Write-Host "Alias (Wildcard): $name <- $($treffer.Name)" }
            }
            if (!(Test-Path $ziel)) { Write-Host "WARNUNG: $name nicht aufloesbar" }
          }

          echo "LLVM_SYS_181_PREFIX=$condaPrefix\Library" >> $env:GITHUB_ENV
          echo "LIBCLANG_PATH=$condaPrefix\Library\bin" >> $env:GITHUB_ENV
          echo "VCPKGRS_DYNAMIC=1" >> $env:GITHUB_ENV
          echo "INCLUDE=$condaPrefix\Library\include;$vcpkgRoot\include;$env:INCLUDE" >> $env:GITHUB_ENV
          echo "LIB=$condaPrefix\Library\lib;$vcpkgRoot\lib;$env:LIB" >> $env:GITHUB_ENV

          # Git for Windows bringt ebenfalls eine link.exe mit (GNU coreutils).
          # Cargo und clang muessen zwingend den MSVC-Linker davor finden.
          $msvcBin = Join-Path $env:VCToolsInstallDir "bin\HostX64\x64"
          $msvcLink = Join-Path $msvcBin "link.exe"
          if (!(Test-Path $msvcLink)) { throw "MSVC link.exe nicht gefunden: $msvcLink" }
          echo "CARGO_TARGET_X86_64_PC_WINDOWS_MSVC_LINKER=$msvcLink" >> $env:GITHUB_ENV
          echo "PATH=$condaPrefix\Library\bin;$condaPrefix\Scripts;$vcpkgRoot\bin;$msvcBin;$env:PATH" >> $env:GITHUB_ENV

      - name: Install macOS deps (LLVM, SDL2, sqlite, curl)
        if: matrix.os == 'macos-latest'
        run: |
          # Cocoa/AppKit/Foundation sind im System. Wir brauchen nur
          # LLVM (fuer inkwell) und die C-Libraries der Core-Runtime.
          brew update
          brew install llvm@18 sdl2 sdl2_image
          # libcurl und sqlite3 sind im System vorhanden (macOS SDK).
          echo "LIBCLANG_PATH=$(brew --prefix llvm@18)/lib" >> $GITHUB_ENV
          echo "LLVM_SYS_181_PREFIX=$(brew --prefix llvm@18)" >> $GITHUB_ENV
          echo "PKG_CONFIG_PATH=$(brew --prefix)/lib/pkgconfig:$(brew --prefix llvm@18)/lib/pkgconfig" >> $GITHUB_ENV
          # SDL2-Header liegen unter $(brew --prefix sdl2)/include, aber der
          # cc-Build fuer runtime/moo_graphics.c setzt kein -I dort. Via CPATH
          # und LIBRARY_PATH exposen, damit #include <SDL2/SDL.h> aufloest und
          # der Linker libSDL2 findet. ZUSAETZLICH muss .../include/SDL2 im
          # Include-Path sein, weil SDL_image.h intern `#include "SDL.h"`
          # (ohne SDL2/-Prefix) macht.
          SDL2_PREFIX="$(brew --prefix sdl2)"
          SDL2_IMAGE_PREFIX="$(brew --prefix sdl2_image)"
          echo "CPATH=${SDL2_PREFIX}/include:${SDL2_PREFIX}/include/SDL2:${SDL2_IMAGE_PREFIX}/include:${SDL2_IMAGE_PREFIX}/include/SDL2:${CPATH}" >> $GITHUB_ENV
          echo "LIBRARY_PATH=${SDL2_PREFIX}/lib:${SDL2_IMAGE_PREFIX}/lib:${LIBRARY_PATH}" >> $GITHUB_ENV
          echo "BINDGEN_EXTRA_CLANG_ARGS=-I${SDL2_PREFIX}/include -I${SDL2_PREFIX}/include/SDL2 -I${SDL2_IMAGE_PREFIX}/include -I${SDL2_IMAGE_PREFIX}/include/SDL2" >> $GITHUB_ENV
          # MACOSX_DEPLOYMENT_TARGET fuer cc-Build UND rustc-Link angleichen —
          # sonst warnt ld: 'object file built for newer macOS version (15.5)
          # than being linked (15.0)'. Dynamisch = ueberlebt Runner-Image-Bumps.
          echo "MACOSX_DEPLOYMENT_TARGET=$(sw_vers -productVersion | cut -d. -f1).0" >> $GITHUB_ENV

      # ---------------------------------------------------------------------
      # Rust Toolchain
      # ---------------------------------------------------------------------
      - name: Rust toolchain (stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo registry + target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            compiler/target
          key: ${{ runner.os }}-ui-cargo-${{ hashFiles('compiler/Cargo.toml', 'compiler/build.rs') }}
          restore-keys: |
            ${{ runner.os }}-ui-cargo-

      # ---------------------------------------------------------------------
      # Build — UI-Modul ohne 3D-Backend (--no-default-features + moo_ui)
      # ---------------------------------------------------------------------
      - name: Cargo build (release, moo_ui only)
        working-directory: compiler
        run: cargo build --release --no-default-features --features moo_ui

      # ---------------------------------------------------------------------
      # Compile ui_login_demo.moos → IR
      # ---------------------------------------------------------------------
      - name: moo-compiler — emit IR (ui_login_demo)
        shell: bash
        run: |
          ./compiler/target/release/moo-compiler${{ matrix.exe_suffix }} \
            compile beispiele/ui_login_demo.moos --emit-ir -o ui_login_demo.ll
          test -s ui_login_demo.ll
          echo "IR-Groesse: $(wc -c < ui_login_demo.ll) Bytes"

      # ---------------------------------------------------------------------
      # Compile ui_login_demo.moos → Binary (Linker-Smoke)
      # ---------------------------------------------------------------------
      - name: moo-compiler — build binary (ui_login_demo)
        shell: bash
        run: |
          ./compiler/target/release/moo-compiler${{ matrix.exe_suffix }} \
            compile beispiele/ui_login_demo.moos -o ui_login_demo${{ matrix.exe_suffix }}
          ls -la ui_login_demo${{ matrix.exe_suffix }}

      # ---------------------------------------------------------------------
      # Smoke-Test — Binary starten und nach kurzer Zeit killen.
      # Exit-Code ist irrelevant, wichtig ist dass der Prozess "lebt".
      # ---------------------------------------------------------------------
      - name: Smoke-Test (Linux, Xvfb, timeout 3s)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get install -y xvfb
          # rc=124 = timeout ausgeloest → Prozess lebte bis zum Timeout = OK.
          # rc=0   = Prozess beendete sich selbst sauber       = auch OK.
          # alles andere                                       = Fehler.
          set +e
          xvfb-run -a timeout 3 ./ui_login_demo
          rc=$?
          set -e
          echo "Exit-Code: $rc"
          if [ $rc -ne 124 ] && [ $rc -ne 0 ]; then
            echo "::error::Smoke-Test Linux fehlgeschlagen (rc=$rc)"
            exit $rc
          fi

      - name: Smoke-Test (Windows, Process lebt 2s)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $proc = Start-Process -FilePath ".\ui_login_demo.exe" -PassThru
          Start-Sleep -Seconds 2
          if ($proc.HasExited) {
            Write-Host "::error::Prozess endete nach <2s — ExitCode=$($proc.ExitCode)"
            exit 1
          }
          Write-Host "Prozess lebt nach 2s — OK. Beende ihn nun."
          Stop-Process -Id $proc.Id -Force
          exit 0

      - name: Native Win32 UI API + Visual Regression Suite
        if: matrix.os == 'windows-latest'
        shell: bash
        env:
          MOO_COMPILER: ./compiler/target/release/moo-compiler.exe
          MOO_UI_BACKEND: win32
        run: bash scripts/ui-test-runner.sh

      - name: Upload Win32 UI test evidence
        if: matrix.os == 'windows-latest' && always()
        uses: actions/upload-artifact@v4
        with:
          name: moo-win32-ui-test-evidence
          path: |
            beispiele/snapshots/**/frame_*.png
            beispiele/snapshots/**/frame_*.json
            beispiele/snapshots/**/run.log
            beispiele/snapshots/test_report.json
          if-no-files-found: error
          retention-days: 30

      - name: Bundle portable Moo SDK (Windows x64)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $sdk = Join-Path $PWD "moo-sdk-windows-x64"
          New-Item -ItemType Directory -Force -Path $sdk, "$sdk\bin", "$sdk\lib", "$sdk\toolchain\bin" | Out-Null

          Copy-Item ".\compiler\target\release\moo-compiler.exe" "$sdk\moo-compiler.exe"
          Copy-Item ".\stdlib" "$sdk\stdlib" -Recurse

          $runtime = Get-ChildItem ".\compiler\target\release\build\moo-compiler-*\out\moo_runtime.lib" |
            Sort-Object LastWriteTime -Descending | Select-Object -First 1
          if (!$runtime) { throw "moo_runtime.lib nicht gefunden" }
          Copy-Item $runtime.FullName "$sdk\lib\moo_runtime.lib"

          $clang = Get-Command clang.exe -ErrorAction Stop
          $lld = Get-Command lld-link.exe -ErrorAction Stop
          Copy-Item $clang.Source "$sdk\toolchain\bin\clang.exe"
          Copy-Item $lld.Source "$sdk\toolchain\bin\lld-link.exe"

          # Alle aktiven Link-Suchpfade flach bündeln: conda/vcpkg sowie
          # MSVC-, UCRT- und Windows-SDK-Importbibliotheken.
          foreach ($dir in ($env:LIB -split ';' | Where-Object { $_ -and (Test-Path $_) })) {
            Get-ChildItem $dir -Filter "*.lib" -File -ErrorAction SilentlyContinue |
              Copy-Item -Destination "$sdk\lib" -Force
          }

          # Clang-Builtins für lld-link ohne installierte LLVM-Toolchain.
          $resourceDir = (& $clang.Source --print-resource-dir).Trim()
          Get-ChildItem $resourceDir -Recurse -Filter "clang_rt.builtins*.lib" -File -ErrorAction SilentlyContinue |
            Copy-Item -Destination "$sdk\lib" -Force

          $condaBin = Join-Path $env:LLVM_SYS_181_PREFIX "bin"
          $vcpkgBin = "$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows\bin"
          foreach ($dir in @($condaBin, $vcpkgBin)) {
            if (Test-Path $dir) {
              Get-ChildItem $dir -Filter "*.dll" -File |
                Copy-Item -Destination "$sdk\bin" -Force
            }
          }
          # clang.exe lädt seine LLVM-DLLs bevorzugt direkt neben sich.
          Get-ChildItem $condaBin -Filter "*.dll" -File |
            Copy-Item -Destination "$sdk\toolchain\bin" -Force

          if (!$env:VCToolsRedistDir -or !(Test-Path $env:VCToolsRedistDir)) {
            throw "VCToolsRedistDir fehlt — portables SDK darf keine CRT-DLLs still auslassen"
          }
          Get-ChildItem $env:VCToolsRedistDir -Recurse -Filter "*.dll" -File |
            Where-Object { $_.FullName -match '\\x64\\' } |
            Copy-Item -Destination "$sdk\bin" -Force
          foreach ($requiredDll in @("vcruntime140.dll", "msvcp140.dll")) {
            if (!(Test-Path (Join-Path "$sdk\bin" $requiredDll))) {
              throw "Pflicht-DLL fehlt im SDK: $requiredDll"
            }
          }

          @'
          @echo off
          set "MOO_HOME=%~dp0"
          set "PATH=%~dp0bin;%~dp0toolchain\bin;%PATH%"
          "%~dp0moo-compiler.exe" %*
          '@ | Set-Content "$sdk\moo.cmd" -Encoding ASCII

          @'
          @echo off
          set "MOO_HOME=%~dp0"
          set "PATH=%~dp0bin;%~dp0toolchain\bin;%PATH%"
          cmd /K
          '@ | Set-Content "$sdk\moo-shell.cmd" -Encoding ASCII

          # Selftest ohne geerbte Visual-Studio-/Runner-Linkumgebung.
          $selftest = Join-Path $sdk "sdk_selftest.moos"
          'zeige "MOO-WINDOWS-SDK-RUN-OK"' | Set-Content $selftest -Encoding UTF8
          $savedPath = $env:PATH
          $savedLib = $env:LIB
          $savedInclude = $env:INCLUDE
          $env:PATH = "$sdk\bin;$sdk\toolchain\bin;$env:SystemRoot\System32;$env:SystemRoot"
          Remove-Item Env:LIB -ErrorAction SilentlyContinue
          Remove-Item Env:INCLUDE -ErrorAction SilentlyContinue
          Remove-Item Env:VCToolsInstallDir -ErrorAction SilentlyContinue

          & "$sdk\moo.cmd" run $selftest
          if ($LASTEXITCODE -ne 0) { throw "Portables SDK: moo run fehlgeschlagen ($LASTEXITCODE)" }
          $selfExe = Join-Path $sdk "sdk_selftest.exe"
          & "$sdk\moo.cmd" compile $selftest -o $selfExe
          if ($LASTEXITCODE -ne 0) { throw "Portables SDK: compile fehlgeschlagen ($LASTEXITCODE)" }
          & $selfExe
          if ($LASTEXITCODE -ne 0) { throw "Portables SDK: erzeugte EXE fehlgeschlagen ($LASTEXITCODE)" }

          $env:PATH = $savedPath
          $env:LIB = $savedLib
          $env:INCLUDE = $savedInclude

          Get-ChildItem $sdk
          Get-ChildItem "$sdk\lib" -Filter "*.lib" | Measure-Object
          Get-ChildItem "$sdk\bin" -Filter "*.dll" | Measure-Object

      - name: Upload portable Moo SDK (Windows x64)
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: moo-sdk-windows-x64
          path: moo-sdk-windows-x64
          if-no-files-found: error
          retention-days: 30

      - name: Smoke-Test (macOS, Process lebt 2s)
        if: matrix.os == 'macos-latest'
        run: |
          ./ui_login_demo &
          PID=$!
          sleep 2
          if kill -0 $PID 2>/dev/null; then
            echo "Prozess $PID lebt nach 2s — OK."
            kill $PID 2>/dev/null || true
            wait $PID 2>/dev/null || true
            exit 0
          else
            echo "::error::Prozess endete nach <2s"
            exit 1
          fi

      # ---------------------------------------------------------------------
      # Bei Fehler: Logs und Zwischenprodukte hochladen
      # ---------------------------------------------------------------------
      - name: Upload artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ui-ci-${{ matrix.os }}-artifacts
          path: |
            ui_login_demo.ll
            ui_login_demo
            ui_login_demo.exe
            compiler/target/release/build/**/output
            compiler/target/release/build/**/stderr
          if-no-files-found: ignore
          retention-days: 14
